spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  jpa:
#    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    web: debug
    sql: trace